# 매주 월요일 오후 KENCIS 인증 수집을 시도한다. 한 슬롯이 성공하면 이후 슬롯은
# 건너뛰고, 앞선 슬롯의 API 장애는 마지막 슬롯까지 복구 기회를 남긴다.
name: sync-kencis

on:
  schedule:
    - cron: '17 4 * * 1' # 매주 월요일 13:17 KST
    - cron: '17 7 * * 1' # 매주 월요일 16:17 KST
    - cron: '17 10 * * 1' # 매주 월요일 19:17 KST (최종 시도)
  workflow_dispatch:
    inputs:
      review-only:
        description: '열린 sync/kencis PR만 재검증 (재수집·매핑·PR 갱신 없음)'
        type: boolean
        default: false

concurrency:
  group: sync-kencis
  cancel-in-progress: false

jobs:
  prepare:
    if: ${{ !inputs.review-only }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    outputs:
      fetch-succeeded: ${{ steps.fetch.outputs.succeeded }}
      candidate-count: ${{ steps.candidates.outputs.count }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: check earlier successful sync slot
        id: recovery
        uses: actions/github-script@v9
        with:
          script: |
            const { pathToFileURL } = require('node:url');
            const workflow = await import(pathToFileURL(
              `${process.env.GITHUB_WORKSPACE}/scripts/sync-workflow.mjs`,
            ).href);
            await workflow.checkEarlierSuccessfulSync({ github, context, core });
      - uses: actions/setup-node@v7
        if: steps.recovery.outputs.should-run == 'true'
        with:
          node-version: 22
      - name: preserve previous review list
        if: steps.recovery.outputs.should-run == 'true'
        run: cp data/unmapped.json "$RUNNER_TEMP/unmapped-before.json"
      - name: fetch KENCIS with short retries
        if: steps.recovery.outputs.should-run == 'true'
        id: fetch
        env:
          DATA_GO_KR_KEY: ${{ secrets.DATA_GO_KR_KEY }}
          EVENT_NAME: ${{ github.event_name }}
          SCHEDULE: ${{ github.event.schedule }}
          FINAL_SCHEDULE: '17 10 * * 1'
        run: |
          if node scripts/fetch-kencis.mjs; then
            echo "succeeded=true" >> "$GITHUB_OUTPUT"
            exit 0
          else
            status=$?
          fi

          echo "succeeded=false" >> "$GITHUB_OUTPUT"
          if [[ "$EVENT_NAME" == "schedule" && "$SCHEDULE" != "$FINAL_SCHEDULE" ]]; then
            echo "::warning::KENCIS API 연결 실패; 다음 복구 슬롯에서 다시 시도합니다."
            {
              echo '## KENCIS 동기화 연기'
              echo
              echo "- 예약: \`$SCHEDULE\`"
              echo '- 결과: 공공데이터 API 연결 실패'
              echo '- 조치: 이번 실행의 빌드·AI·PR 생성을 건너뛰고 다음 복구 슬롯에서 재시도'
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "::error::KENCIS API 연결이 최종 시도까지 실패했습니다."
          exit "$status"
      - name: build and validate raw sync
        if: steps.fetch.outputs.succeeded == 'true'
        run: npm run check
      - name: find new or changed unmapped and ambiguous names
        if: steps.fetch.outputs.succeeded == 'true'
        id: candidates
        run: |
          count=$(node scripts/find-new-unmapped.mjs \
            "$RUNNER_TEMP/unmapped-before.json" \
            data/unmapped.json \
            sync-candidates.json)
          echo "count=$count" >> "$GITHUB_OUTPUT"
          echo "New review candidates: $count"
      - name: upload raw sync
        if: steps.fetch.outputs.succeeded == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: raw-sync
          retention-days: 1
          if-no-files-found: error
          path: |
            data/raw/kencis-import.json
            data/raw/kencis-domestic.json
            data/models.json
            data/models.lite.json
            data/models.min.json
            data/unmapped.json
            mapping/models.json
      - name: upload AI candidates
        if: steps.fetch.outputs.succeeded == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: sync-candidates
          retention-days: 1
          if-no-files-found: error
          path: sync-candidates.json

  enrich:
    needs: prepare
    if: needs.prepare.outputs.fetch-succeeded == 'true' && needs.prepare.outputs.candidate-count != '0'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      proposal: ${{ steps.codex.outputs.final-message }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v8
        with:
          name: raw-sync
          path: .
      - uses: actions/download-artifact@v8
        with:
          name: sync-candidates
          path: .
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: collect hosted web evidence for candidates
        id: evidence
        timeout-minutes: 9
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          EVIDENCE_MODEL: ${{ vars.CODEX_SYNC_MODEL || 'gpt-6-sol' }}
        run: node scripts/collect-sync-evidence.mjs enrich sync-evidence.json
      - uses: actions/upload-artifact@v7
        with:
          name: enrichment-evidence
          path: sync-evidence.json
          retention-days: 7
          if-no-files-found: error
      - name: report unavailable enrichment evidence
        if: steps.evidence.outputs.usable != 'true'
        run: |
          echo '::error::웹 근거 수집 실패; 원본 동기화 PR만 게시하고 자동 보완은 생략합니다.'
          exit 1
      # 보안 경계: 이 잡은 쓰기 토큰이 없고 Codex도 read-only다. 반드시 마지막 단계로 둔다.
      - name: research mapping proposals
        id: codex
        if: steps.evidence.outputs.usable == 'true'
        continue-on-error: true
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          model: ${{ vars.CODEX_SYNC_MODEL || 'gpt-6-sol' }}
          prompt-file: .github/codex/prompts/enrich-kencis.md
          output-schema-file: .github/codex/schemas/enrichment.schema.json
          sandbox: read-only
          codex-args: '["-c", "web_search=\"disabled\"", "--ephemeral"]'
          effort: high
          allow-bots: true

  publish:
    needs: [prepare, enrich]
    if: always() && needs.prepare.result == 'success' && needs.prepare.outputs.fetch-succeeded == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      pull-request-number: ${{ steps.pr.outputs.pull-request-number }}
      pull-request-url: ${{ steps.pr.outputs.pull-request-url }}
      pull-request-head-sha: ${{ steps.pr.outputs.pull-request-head-sha }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: preserve base data for PR summary
        run: |
          mkdir -p \
            "$RUNNER_TEMP/sync-base/data/raw" \
            "$RUNNER_TEMP/sync-base/data" \
            "$RUNNER_TEMP/sync-base/mapping"
          cp data/raw/kencis-import.json "$RUNNER_TEMP/sync-base/data/raw/kencis-import.json"
          cp data/raw/kencis-domestic.json "$RUNNER_TEMP/sync-base/data/raw/kencis-domestic.json"
          cp data/models.json "$RUNNER_TEMP/sync-base/data/models.json"
          cp data/unmapped.json "$RUNNER_TEMP/sync-base/data/unmapped.json"
          cp mapping/models.json "$RUNNER_TEMP/sync-base/mapping/models.json"
      - uses: actions/download-artifact@v8
        with:
          name: raw-sync
          path: .
      - uses: actions/download-artifact@v8
        with:
          name: sync-candidates
          path: .
      - name: apply validated AI proposal with raw-sync fallback
        id: apply
        env:
          AI_PROPOSAL: ${{ needs.enrich.outputs.proposal }}
          CANDIDATE_COUNT: ${{ needs.prepare.outputs.candidate-count }}
        run: |
          cp mapping/models.json "$RUNNER_TEMP/models-before-ai.json"
          status=not-needed

          if [[ "$CANDIDATE_COUNT" != "0" ]]; then
            if [[ -n "$AI_PROPOSAL" ]]; then
              AI_PROPOSAL_PATH="$RUNNER_TEMP/ai-proposal.json" \
                node -e 'require("node:fs").writeFileSync(process.env.AI_PROPOSAL_PATH, process.env.AI_PROPOSAL)'
              if status=$(node scripts/apply-ai-mappings.mjs \
                sync-candidates.json \
                "$RUNNER_TEMP/ai-proposal.json" \
                "$RUNNER_TEMP/ai-enrichment.md"); then
              else
                cp "$RUNNER_TEMP/models-before-ai.json" mapping/models.json
                status=fallback-invalid-proposal
              fi
            else
              status=fallback-ai-unavailable
            fi
          fi

          if ! npm run check; then
            if [[ "$status" == "applied" ]]; then
              echo "::warning::AI mapping failed repository checks; reverting to raw sync"
              cp "$RUNNER_TEMP/models-before-ai.json" mapping/models.json
              npm run check
              status=fallback-check-failed
              rm -f "$RUNNER_TEMP/ai-enrichment.md"
            else
              exit 1
            fi
          fi

          if [[ ! -s "$RUNNER_TEMP/ai-enrichment.md" ]]; then
            printf '%s\n' \
              '### AI 사전 보완' \
              '' \
              "- 결과: ${status}" \
              "- 신규 검토 후보: ${CANDIDATE_COUNT}건" \
              > "$RUNNER_TEMP/ai-enrichment.md"
          fi

          rm -f sync-candidates.json
          unexpected=$(git status --porcelain | awk '{print $2}' | grep -Ev \
            '^(mapping/models\.json|data/raw/kencis-(import|domestic)\.json|data/models(\.lite|\.min)?\.json|data/unmapped\.json)$' || true)
          if [[ -n "$unexpected" ]]; then
            echo "::error::Unexpected changed paths:"
            echo "$unexpected"
            exit 1
          fi

          echo "status=$status" >> "$GITHUB_OUTPUT"
      - name: summarize actual data changes
        run: |
          node scripts/summarize-sync.mjs \
            "$RUNNER_TEMP/sync-base" \
            . \
            "$RUNNER_TEMP/sync-summary.md"
      - name: compose pull request body
        env:
          AI_STATUS: ${{ steps.apply.outputs.status }}
        run: |
          node scripts/sync-workflow.mjs \
            "$RUNNER_TEMP/sync-summary.md" \
            "$RUNNER_TEMP/ai-enrichment.md" \
            "$AI_STATUS" \
            "$RUNNER_TEMP/pr-body.md"
      - name: create or update sync PR
        id: pr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ github.token }}
          branch: sync/kencis
          delete-branch: true
          title: 'sync: 신규 KENCIS 인증 반영'
          commit-message: |
            sync: refresh KENCIS certification data

            Co-authored-by: Codex <noreply@openai.com>
          author: 'starhn87 <36434219+starhn87@users.noreply.github.com>'
          committer: 'starhn87 <36434219+starhn87@users.noreply.github.com>'
          body-path: ${{ runner.temp }}/pr-body.md
          add-paths: |
            mapping/models.json
            data/raw/kencis-import.json
            data/raw/kencis-domestic.json
            data/models.json
            data/models.lite.json
            data/models.min.json
            data/unmapped.json

  review-target:
    needs: publish
    if: always() && (inputs.review-only || needs.publish.outputs.pull-request-number != '')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      number: ${{ steps.target.outputs.number }}
      head-sha: ${{ steps.target.outputs.head-sha }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: resolve current sync PR
        id: target
        uses: actions/github-script@v9
        with:
          script: |
            const { pathToFileURL } = require('node:url');
            const workflow = await import(pathToFileURL(
              `${process.env.GITHUB_WORKSPACE}/scripts/sync-workflow.mjs`,
            ).href);
            const target = await workflow.resolveSyncReviewTarget({ github, context });
            core.setOutput('number', target.number);
            core.setOutput('head-sha', target.headSha);

  review-evidence:
    needs: review-target
    if: always() && needs.review-target.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      usable: ${{ steps.evidence.outputs.usable }}
    steps:
      # API 키를 쓰는 코드는 PR이 아니라 실행 시점의 신뢰된 워크플로에서만 가져온다.
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: collect independent hosted web evidence for PR HEAD
        id: evidence
        timeout-minutes: 9
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          EVIDENCE_MODEL: ${{ vars.CODEX_SYNC_MODEL || 'gpt-6-sol' }}
          REVIEW_HEAD_SHA: ${{ needs.review-target.outputs.head-sha }}
        run: node scripts/collect-sync-evidence.mjs review sync-evidence.json
      - uses: actions/upload-artifact@v7
        with:
          name: review-evidence
          path: sync-evidence.json
          retention-days: 7
          if-no-files-found: error
      - name: report unavailable review evidence
        if: steps.evidence.outputs.usable != 'true'
        run: |
          echo '::error::웹 근거 수집 실패; 머지 가능 판정을 내리지 않습니다. artifact와 수동 검토가 필요합니다.'
          exit 1

  review:
    needs: [review-target, review-evidence]
    if: always() && needs.review-target.result == 'success' && needs.review-evidence.outputs.usable == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      result: ${{ steps.codex.outputs.final-message }}
      outcome: ${{ steps.codex.outcome }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: preserve trusted review instructions
        run: |
          cp .github/codex/prompts/review-kencis.md "$RUNNER_TEMP/review-kencis.md"
          cp .github/codex/schemas/review.schema.json "$RUNNER_TEMP/review.schema.json"
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.review-target.outputs.head-sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/download-artifact@v8
        with:
          name: review-evidence
          path: .
      # 보안 경계: 생성된 PR HEAD를 읽기만 하며 이 잡은 쓰기 토큰이 없다. 반드시 마지막 단계로 둔다.
      - name: independently review sync PR
        id: codex
        continue-on-error: true
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          model: ${{ vars.CODEX_SYNC_MODEL || 'gpt-6-sol' }}
          prompt-file: ${{ runner.temp }}/review-kencis.md
          output-schema-file: ${{ runner.temp }}/review.schema.json
          sandbox: read-only
          codex-args: '["-c", "web_search=\"disabled\"", "--ephemeral"]'
          effort: medium
          allow-bots: true

  comment:
    needs: [review-target, review]
    if: always() && needs.review-target.outputs.number != ''
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      # 쓰기 토큰을 가진 잡에서는 PR 코드가 아닌 실행 시점의 신뢰된 워크플로 코드를 사용한다.
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: create or update AI verdict comment
        uses: actions/github-script@v9
        env:
          PR_NUMBER: ${{ needs.review-target.outputs.number }}
          REVIEWED_HEAD_SHA: ${{ needs.review-target.outputs.head-sha }}
          REVIEW_JSON: ${{ needs.review.outputs.result }}
          REVIEW_JOB_RESULT: ${{ needs.review.outputs.outcome || needs.review.result }}
        with:
          script: |
            const { pathToFileURL } = require('node:url');
            const workflow = await import(pathToFileURL(
              `${process.env.GITHUB_WORKSPACE}/scripts/sync-workflow.mjs`,
            ).href);
            await workflow.upsertReviewComment({
              github,
              context,
              pullRequestNumber: Number(process.env.PR_NUMBER),
              reviewJson: process.env.REVIEW_JSON,
              reviewJobResult: process.env.REVIEW_JOB_RESULT,
              reviewedHeadSha: process.env.REVIEWED_HEAD_SHA,
            });
